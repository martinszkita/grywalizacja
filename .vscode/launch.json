{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "utils.py",
            "type": "python",
            "request": "launch",

            // WAŻNE!
            "module": "gamification.quiz.utils",
            "cwd": "${workspaceFolder}/gamification",

            "env": {
                "DJANGO_SETTINGS_MODULE": "gamification.settings"
            },
            "justMyCode": true
        }
    ]
}
